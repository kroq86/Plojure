It handles conditional statements, evident from the implementation of the is-prime function .
It supports basic arithmetic operations, shown in the Fibonacci and prime number calculations .
It has function definitions and function calls, as seen in the definitions of fib, print-fib, is-prime, and print-primes .
It appears to support lambda expressions, which are a key component of Turing completeness in Lisp .